bash

cat > /mnt/user-data/outputs/build.yml << 'WORKFLOW'
name: Build Android APK (Acamay EV Pro)

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Node.js 18
      uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Android SDK
      uses: android-actions/setup-android@v3
      with:
        log-accepted-licenses: true

    - name: Paketleri Kur
      run: |
        rm -rf android package.json package-lock.json node_modules .gradle
        npm init -y
        npm install \
          @capacitor/core@5.7.0 \
          @capacitor/cli@5.7.0 \
          @capacitor/android@5.7.0 \
          @capacitor-community/bluetooth-le@3.1.2 \
          @capacitor/network@5.0.7 \
          cordova-res \
          --save-exact

    - name: Capacitor Init
      run: npx cap init "Acamay EV Pro" "com.acamay.evsarj" --web-dir=www

    - name: capacitor.config.json Oluştur
      run: |
        cat > capacitor.config.json << 'JSON'
        {
          "appId": "com.acamay.evsarj",
          "appName": "Acamay EV Pro",
          "webDir": "www",
          "android": {
            "allowMixedContent": true,
            "useLegacyBridge": false
          },
          "server": {
            "androidScheme": "http",
            "cleartext": true
          },
          "plugins": {
            "BluetoothLe": {
              "displayStrings": {
                "scanning": "BLE Cihazlar Aranıyor...",
                "cancel": "İptal",
                "availableDevices": "Bulunan Cihazlar",
                "noDeviceFound": "Cihaz Bulunamadı"
              }
            }
          }
        }
        JSON

    - name: Android Platform Ekle
      run: npx cap add android

    - name: AndroidManifest.xml Güncelle
      run: |
        cat > android/app/src/main/AndroidManifest.xml << 'MANIFEST'
        <?xml version="1.0" encoding="utf-8"?>
        <manifest xmlns:android="http://schemas.android.com/apk/res/android">

          <!-- İnternet ve WiFi -->
          <uses-permission android:name="android.permission.INTERNET" />
          <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
          <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
          <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

          <!-- Bluetooth (Android 12+) -->
          <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
          <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30"/>
          <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation"/>
          <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
          <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
          <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
          <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

          <!-- Ses ve Bildirimler -->
          <uses-permission android:name="android.permission.VIBRATE" />
          <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
          <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

          <!-- BLE özelliği -->
          <uses-feature android:name="android.hardware.bluetooth_le" android:required="false"/>

          <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="Acamay EV Pro"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true"
            android:networkSecurityConfig="@xml/network_security_config">

            <activity
              android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
              android:name=".MainActivity"
              android:label="Acamay EV Pro"
              android:launchMode="singleTask"
              android:exported="true"
              android:theme="@style/AppTheme.NoActionBarLaunch"
              android:windowSoftInputMode="adjustResize">
              <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
              </intent-filter>
            </activity>

            <provider
              android:name="androidx.core.content.FileProvider"
              android:authorities="${applicationId}.fileprovider"
              android:exported="false"
              android:grantUriPermissions="true">
              <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
            </provider>
          </application>
        </manifest>
        MANIFEST

    - name: Network Security Config Ekle
      run: |
        mkdir -p android/app/src/main/res/xml
        cat > android/app/src/main/res/xml/network_security_config.xml << 'XML'
        <?xml version="1.0" encoding="utf-8"?>
        <network-security-config>
          <base-config cleartextTrafficPermitted="true">
            <trust-anchors>
              <certificates src="system"/>
            </trust-anchors>
          </base-config>
          <domain-config cleartextTrafficPermitted="true">
            <domain includeSubdomains="true">192.168.1.88</domain>
            <domain includeSubdomains="true">192.168.0.0</domain>
            <domain includeSubdomains="true">10.0.0.0</domain>
            <domain includeSubdomains="true">localhost</domain>
          </domain-config>
        </network-security-config>
        XML

    - name: İkon ve Splash Oluştur
      run: |
        mkdir -p resources www
        # Logo yoksa programatik oluştur
        if [ ! -f icon.png ]; then
          python3 -c "
        import struct, zlib

        def create_png(width, height, pixels):
            def write_chunk(chunk_type, data):
                c = chunk_type + data
                return struct.pack('>I', len(data)) + c + struct.pack('>I', zlib.crc32(c) & 0xffffffff)
            
            raw = b''
            for row in pixels:
                raw += b'\x00'
                for r,g,b,a in row:
                    raw += bytes([r,g,b,a])
            
            compressed = zlib.compress(raw)
            ihdr = struct.pack('>IIBBBBB', width, height, 8, 2, 0, 0, 0)
            # RGBA
            ihdr = struct.pack('>II', width, height) + bytes([8,6,0,0,0])
            
            png = b'\x89PNG\r\n\x1a\n'
            png += write_chunk(b'IHDR', ihdr)
            
            raw2 = b''
            for row in pixels:
                raw2 += b'\x00'
                for r,g,b,a in row:
                    raw2 += bytes([r,g,b,a])
            
            png += write_chunk(b'IDAT', zlib.compress(raw2))
            png += write_chunk(b'IEND', b'')
            return png

        W = 512
        pixels = []
        for y in range(W):
            row = []
            for x in range(W):
                dx = x - W//2
                dy = y - W//2
                dist = (dx*dx + dy*dy)**0.5
                if dist < W//2 - 20:
                    # Yeşil-mavi gradient arka plan
                    ratio = dist / (W//2)
                    r = int(0 + ratio * 8)
                    g = int(184 - ratio * 50)
                    b2 = int(144 + ratio * 80)
                    row.append((r, g, b2, 255))
                elif dist < W//2:
                    row.append((255,255,255,200))
                else:
                    row.append((0,0,0,0))
            pixels.append(row)

        # Yıldırım şekli (basit)
        for y in range(W):
            for x in range(W):
                dx = x - W//2
                dy = y - W//2
                # Merkez bölge - şimşek
                if abs(dx) < 60 and abs(dy) < 160:
                    if dx > -20 and dy < 20:  # Üst sağ
                        pixels[y][x] = (255, 255, 255, 255)
                    elif dx < 20 and dy > -20:  # Alt sol  
                        pixels[y][x] = (255, 255, 255, 255)

        with open('icon.png', 'wb') as f:
            f.write(create_png(W, W, pixels))
        print('icon.png olusturuldu')
          "
        fi
        cp icon.png resources/icon.png 2>/dev/null || true
        [ -f splash.png ] && cp splash.png resources/splash.png || cp icon.png resources/splash.png
        npx cordova-res android --skip-config --copy || echo "cordova-res atlandı"

    - name: JAVA_HOME Ayarla
      run: |
        echo "JAVA_HOME=$JAVA_HOME_17_X64" >> $GITHUB_ENV
        echo "org.gradle.java.home=$JAVA_HOME_17_X64" >> android/gradle.properties

    - name: Web Dosyalarını Kopyala
      run: npx cap copy

    - name: Capacitor Sync
      run: npx cap sync android

    - name: Android Lisansları
      run: |
        cd android
        mkdir -p "$ANDROID_HOME/licenses"
        yes | sdkmanager --licenses || true

    - name: APK Derle
      run: |
        cd android
        chmod -R +x gradlew
        ./gradlew assembleDebug --no-daemon --stacktrace

    - name: APK Yükle
      uses: actions/upload-artifact@v4
      with:
        name: Acamay-EV-Pro-APK
        path: android/app/build/outputs/apk/debug/app-debug.apk
WORKFLOW
echo "workflow yazıldı"
Output

workflow yazıldı
